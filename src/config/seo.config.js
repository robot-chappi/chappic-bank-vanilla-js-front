const SITE_NAME = 'Chappic Bank - Vanilla JS'

export const getTitle = title => (title ? `${title} | ${SITE_NAME}` : SITE_NAME)

// export const getTitle = title => {
// 	return title ? `${title} | ${SITE_NAME}` : SITE_NAME
// }
